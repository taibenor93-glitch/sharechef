## 2026-08-03 — Hebrew language lock (third language failure — structural fix)
Symptom: Micheli stuck in one language across all of Tai's sessions (Hebrew this week; English-vs-French earlier with a neighbor), on web Safari and the App Store app; recipe dropdown changed nothing; End session / app close changed nothing. Blocked launch-clip filming.
Root cause: language lived only in prompt persuasion, and the prompt argued with itself. The memory summarizer was ordered to "always note which language the user cooks in," the welcome-back greeting opened in that remembered language, and the "stay in the conversation's language / unclear audio never switches" clause consistently beat the "follow the user's live language" clause — so whatever language a session opened in became locked, in both directions. The Home dropdown never touched the voice session at all (typed recipes only, no Hebrew option). Third language-behavior failure (07-26, 07-29, 08-02) → per micheli-troubleshooter, prompt patches no longer acceptable.
Structural fix (deployed this date): deterministic session language in server.js — explicit user pick (sent in the auth frame) > profiles.language (new Supabase column, added 08-03) > device language from Accept-Language header > English; only the user's live speech outranks the pick. Memory is now ordered to never record language and to ignore any language already recorded (existing "cooks in Hebrew" notes self-clean on next merge). The dropdown is now a real voice-language control with Hebrew added, persisted on-device and on the account. Old App Store builds get the device-language default server-side with no update needed. Bonus fix: .topbar safe-area padding — top nav (Preferences/Sign out) was under the iOS status bar and untappable.
Verification rule: only Tai declares this fixed, after he picks English and hears English audio himself.
Also this date: v1.4 confusion resolved — "Ready for Distribution" is Apple's post-2023 name for LIVE (since ~07-31); Tai's phone had v1.4 and was signed in all along (the "no Sign in option" was the Sign out state, hidden under the status bar). OpenAI key follow-up: key value was accidentally displayed in the Claude chat during pre-deploy checks — rotate key + update .env, server/.env, Railway Variables.

## 2026-07-26 — "Micheli doesn't work" (French / forgetting / kicked out)
Symptom: refuses French, comes back in English and restarts, forgets prior cooking, app drops mid-session (both local and Railway).
Root cause 1 (FIXED in server.js, pending deploy): system prompt hard-anchored Micheli to English and the greeting/resume lines were hardcoded English; nothing carried the session language. Rewrote language rules, made greeting/resume language-aware, memory now records the user's cooking language.
Root cause 2 (OPEN): every production voice session this week logged "auth: guest" — the iOS app never authenticates, so memory/resume/dietary never apply there. Web login verified working 17:32 EDT ("auth: user ... profile loaded, memory loaded"). Investigate iOS (Capacitor) session persistence next.
Root cause 3 (OPEN): mid-cook disconnects are client-side (WS close 1005/1006 from the browser/app, OpenAI always closes cleanly afterward). Not an OpenAI or server failure. Investigate iOS WebView socket drops next.
Also noted: Railway has SUPABASE_SERVICE_ROLE_KEY set — server code never uses it and the registry forbids it on this server; delete it. EMAIL_SECRET / NEXT_PUBLIC_APP_URL look like leftovers from another stack.

## 2026-07-26 — v1.4 app fixes (built, awaiting real-iPhone test)
Login persistence: supabaseClient.ts now stores the Supabase session in Capacitor Preferences (iOS UserDefaults) on native builds instead of purgeable WKWebView localStorage. Requires: npm install (adds @capacitor/preferences) + npx cap sync ios. Users must sign in once more after updating.
Voice resilience: realtimeVoice.ts auto-reconnects up to 3 times after an unexpected drop, fetches a fresh auth token on every (re)connect, and asks Micheli to resume the cook. Deliberate "End session" never reconnects.
Visibility: Home.tsx shows under the mic whether Micheli knows the user or the session is guest mode — silent guest sessions are no longer possible.
iOS version bumped to 1.4 (build 9). TypeScript + vite build verified clean in a clean environment. GATE: no App Store submission until Tai confirms on a real iPhone: login survives app restart, voice session reconnects after a drop, and the signed-in line shows under the mic.

2026-07-27 — v1.4 (build 9) submitted to App Review at 10:30 ET. Status: Waiting for Review. Contains: reachable+persistent sign-in, voice auto-reconnect+resume, visible account state, language fixes live server-side since 07-26.

## 2026-07-29 — stuck in Italian across sessions / sessions kept resuming
Symptom: signed-in user (Tai's iPhone, v1.4 test build) greeted in Italian every session; clear attempts to switch language were ignored; ended sessions kept coming back.
Root cause: three mechanisms stacked — memory records the cooking language and the welcome-back greeting opens in it; the language rules said "stay in the conversation's language / unclear audio never switches" with nothing ranking the user's LIVE language above memory or resume; and the 3-hour resume window kept resurrecting saved cooks (the live App Store build can't send the deliberate "bye", so End session looks like a drop).
Fix (commit 8a2768b, deployed via Railway ~12:41 ET): user's current language now explicitly outranks memory, resume, and the opening greeting (3 prompt clauses); RESUME_WINDOW_MS 3h → 20min. Verified by Tai on a fresh session: Micheli switched to English on the first full English sentence.
Recurrence note: second language-behavior failure in 4 days (see 07-26). If language behavior breaks a third time, stop patching prompts and build the structural fix: a scripted multi-language switch test against /api/test/chat run before every deploy.

## 2026-07-30 — no password recovery; users (and Tai) locked out on forgotten password
Symptom: login had password auth but NO "forgot password" path; Tai got logged out on the live iOS build (known WKWebView-purge issue, fixed in v1.4 pending Apple) and could not recover his own account.
Fix (commits ae2181c + 5e69279, deployed via Railway): added src/pages/ResetPassword.tsx (dual-mode: request reset email via supabase.auth.resetPasswordForEmail, then set new password via updateUser), "Forgot password?" link on Login, public /reset route in App.tsx. Supabase: added https://sharechef-production.up.railway.app/reset to Auth > URL Configuration > Redirect URLs (Site URL already correct). Second commit fixed a race where PASSWORD_RECOVERY fired before the page mounted — page now also checks getSession() on mount. Verified end-to-end by Tai: reset email -> link -> set new password -> signed in.
Known follow-ups (post-launch, not blocking): recovery email comes from Supabase's default sender/template (brand it with custom SMTP + template later); reset link opens in browser on iOS, not deep-linked into the app; native-app deep link is a later polish.

## 2026-08-05 — WebSocket heartbeat + resume framing (~60s session drops)
Symptom: voice sessions on Railway were dropping roughly every 60 seconds, cutting Micheli off mid-cook with no warning.
Root cause: Railway idle-closes a quiet WebSocket after about 60s of no traffic (close code 1005), and neither the browser leg nor the OpenAI leg generated any traffic during silent moments (listening, thinking).
Fix (commit 9dca3a1, deployed via Railway): server.js now pings the browser leg every 25s, terminating after 2 missed pongs so the close handler runs cleanly (flushes cook state, closes the OpenAI leg); the OpenAI leg gets its own 25s keep-alive ping and is never terminated from that side by design. realtimeVoice.ts adds reconnect-after-drop: up to 3 attempts with backoff, a fresh auth token fetched on every reconnect, and Micheli briefed to resume the cook rather than restart. This same commit introduced the deliberate-End-session flag and frame, which turned out to carry its own race — see 2026-08-06 below.

## 2026-08-06 — "End session" resumed instead of ending (race in the 08-05 fix)
Symptom: tapping End session still occasionally greeted the next session with "welcome back, picking up where you were" instead of starting fresh.
Root cause: the 08-05 fix cleared cook state only inside the server's close handler, gated on a flag set by parsing the client's "bye" text frame; on a flaky mobile socket the close event could beat that frame's processing, so the flag never got set in time, the transcript got flushed and saved instead of cleared, and the next session loaded that stale cook_sessions row.
Fix (commit eb6cb8b, deployed via Railway): renamed the mechanism to intentionalEnd (client) / end_session (server frame type) and moved the cook-state clear to fire synchronously the instant the server receives the end_session frame, removing the race entirely; the client now also closes with an explicit code 1000. Verified via Railway /health returning {"status":"ok","model":"gpt-realtime","voice":"shimmer","apiKey":true} after deploy.
- 2026-08-13: Shipped screen wake-lock in realtimeVoice.ts (commit 9d4d09c) — screen stays on during active cook; fixes 1-min idle disconnect from screen lock. Server heartbeat (25s ping) already covered Railway idle-close; no server change. Watch: wake-lock denial on low battery falls back to old behavior. iPhone app needs Xcode rebuild to get it.
  Verified 2026-08-13 on iPhone Safari: screen stayed lit through a 2-min idle cook, Micheli responded normally after. Wake-lock fix confirmed working.
- 2026-08-13 (evening): ASC version 1.5 created, build 10 attached, saved — NOT submitted (iPad screenshots pending). FIXED: live App Store description had pasted chat text at top since ~Jul 21 (visible on public listing in 1.3/1.4); replaced with clean copy + new SAFE FOR YOUR TABLE block in the 1.5 draft — goes live when 1.5 ships. Next: iPad simulator screenshots -> upload -> Add for Review. Also due Sept 7: ASC Age Ratings social-media questionnaire.
- 2026-08-13 (night): v1.5 (build 10) SUBMITTED for App Review — auto-release on approval. 4 iPad Pro 13" screenshots (2064x2752) captured via simulator, uploaded to ASC. First iPad-supporting release. Watch: review email within ~48h; after release, verify iPad listing + clean description live. Age Ratings verified COMPLETE 8-13 (4+ / 172 countries, regional exceptions set — Tai was right, done a month ago). Still open: end-session HTTP fallback, playback-flush echo fix, icon-set warning cleanup.
- 2026-08-14: LinkedIn founder post LIVE (1.5/iPad announcement + build-in-public story, App Store link card). Reply to all comments during the day. Next: content-strategy decision, campaign machine, Apple review email watch.
- 2026-08-14 (pm): First $0 Dinner Reel LIVE on Instagram (+FB cross-post) — real app demo, pasta+tomato, math end card. Produced entirely via simulator pipeline (no filming). LinkedIn founder post also live (morning). New doctrine locked: problem/money/time, build-in-public, no ads-style AI clips. Found during production: silence hallucination bug (transcriber invents 'Hello world'/Arabic from quiet mic; Micheli answers phantoms) — TOP of fix list, server patch pending Tai's Go. Daily 4:35pm robot delivers post package from marketing/zero-dollar-dinner/.
- 2026-08-15/16: ANALYTICS PHASE 1 built through 5 review revisions (GPT reviewer loop). Final state: account-bound persistent outbox, 88/88 tests, build clean. Local checkpoint commit 1dc159d (NOT pushed — push would deploy). Both kill switches OFF everywhere. app_events MIGRATION APPLIED to production Supabase (create_app_events + revoke_app_events_client_access): table live, 0 rows, RLS on, zero client privileges, existing tables untouched (5/5/0/4/4). REMAINING NEXT STEP: run scripts/validate-app-events.sql in the Supabase SQL editor (paste whole file, RUN, read PASS/FAIL table) — script is self-cleaning. After validation report → reviewer approves → then (in order, each with Tai's Go): add SUPABASE_SERVICE_KEY to Railway, privacy policy + ASC privacy edits, enable switches, deploy, build 1.6 (bump Xcode MARKETING_VERSION to 1.6; scripts/check-version.mjs gates it). Delivery lesson learned: give Tai files to DRAG into ChatGPT (SHARECHEF-REVISION-5.md pattern), never TextEdit copy-paste.
- 2026-08-15 (night): app_events VALIDATION PASSED — scripts/validate-app-events.sql run in Supabase SQL editor (project ShareChef3, production), 14/14 PASS: valid service insert, dup UUID/identity-link/intro rejected, invalid event/UUID/app_version rejected, anon+authenticated cannot read or insert, privileged insert intact, final row count 0, existing tables unchanged (5/5/0/4/4). Script self-cleaned, zero residue. Kill switches still OFF, nothing pushed or deployed, HEAD still 1dc159d. NEXT (each with Tai's Go, in order): reviewer approval → SUPABASE_SERVICE_KEY to Railway → privacy policy + ASC privacy edits → enable switches → deploy → build 1.6. Note: validate-app-events.sql is still untracked — commit it with the next checkpoint.
- 2026-08-15 (late night): CREDENTIAL AUDIT + LOCAL VERIFICATION both PASSED, reviewer approved. Credential audit: SUPABASE_SERVICE_KEY server-only (server.js:114-116 behind kill-switch guard), zero client/bundle/log/response exposure, user_id JWT-only, invalid JWT = hard 401 no anon fallback, analytics path = single insert into app_events. Decision LOCKED: Option B — key added to Railway only at deploy time (one restart total; Railway env change triggers restart). Local verification: 88/88 tests (session 18, client-events 38 incl. account-bound A→B→A, server 32), tsc clean, vite prod build clean on Tai's Mac (391ms, 117KB gzip), no live services contacted, fresh dist secret-scan clean, VITE_EVENTS_ENABLED unset at build = client switch compiled OFF. Reports: SHARECHEF-CREDENTIAL-AUDIT.md + SHARECHEF-LOCAL-VERIFICATION.md (drag-in pattern). Side effect disclosed: tests regenerated tests/.compiled-events/events.js (tracked seam, shows M). NEXT gated sequence (each with Tai's Go): privacy policy + ASC privacy edits → key at deploy (Option B) → enable switches → deploy → build 1.6. Housekeeping: stale .git/index.lock present — verify no git process then rm before next commit; validate-app-events.sql still untracked.
- 2026-08-15 (session end): PRIVACY INVENTORY + DELETION AUDIT both reviewer-APPROVED. Locked decisions: retention 12 months, tracking NO, ATT NO, shared-device correction = privacy wins (User A deletion removes ALL app_events for A's anon_ids, even other households' analytics rows; functional data of others never touched). CRITICAL FINDING: no in-app account deletion exists (email-only) = Apple 5.1.1(v) gap; must ship in 1.6 BEFORE analytics enable. Blockers: recipes.user_id FK NO ACTION blocks auth deletion; shares schema missing from repo. IN PROGRESS: read-only production schema check — script ready at scripts/inspect-deletion-schema.sql (metadata only: shares columns/FKs, all auth.users FKs, constraint names, pg_cron availability, cron jobs). Tai was mid-run: pbcopy the script, paste in SQL editor (⌘A ⌘V), RUN, export results as markdown. NEXT SESSION: read results → complete DELETION SCHEMA VERIFIED report → reviewer Go → build deletion flow locally + tests. Reports on disk: SHARECHEF-{VALIDATION-REPORT,CREDENTIAL-AUDIT,LOCAL-VERIFICATION,PRIVACY-INVENTORY,DELETION-AUDIT}.md. Still true: switches OFF, nothing pushed/deployed, HEAD 1dc159d.
- 2026-08-16 (morning): PRODUCTION SCHEMA CHECK DONE (read-only, via SQL editor + screen-read). All 6 auth.users FKs confirmed: app_events SET NULL; cook_sessions/micheli_memory/profiles/recipes/shares ALL CASCADE. KEY FINDING: recipes.user_id is CASCADE in production (repo migration stale — says NO ACTION) → deletion blocker GONE. shares schema captured (id uuid PK, user_id uuid FK CASCADE default auth.uid(), recipe_id TEXT not FK, channel, created_at) — migration missing from repo. Verified-engine tables (recipe_verifications/audits/badges) absent from production FK list — likely never applied; hygiene flag. pg_cron AVAILABLE 1.6.4 but NOT enabled, no cron jobs — 12-month purge feasible via migration. Safe deletion sequence locked: collect A's anon_ids → delete app_events (user_id=A OR anon_id in set, privacy-first per reviewer) → admin.deleteUser (cascades all functional) → client local reset. Report: SHARECHEF-SCHEMA-CHECK.md. NEXT: reviewer sign-off → build deletion flow locally (endpoint + Settings UI + reset + tests), no production contact. Still true: switches OFF, HEAD 1dc159d, nothing pushed/deployed.
- 2026-08-16: REVISION 6 BUILT (account deletion + retention, LOCAL ONLY). Reviewer-approved spec implemented: in-app Delete Account in Preferences (password re-entry via signInWithPassword — password never touches our server — + confirm checkbox + full states), server POST /api/account/delete (401/400/403/429 guards, 5-min last_sign_in_at reauth window so refreshed JWTs can't delete, target from verified token ONLY, service-role lazy + kill-switch-independent, hard delete via admin.deleteUser(id,false)), privacy-first analytics purge (all rows for user's anon_ids incl. shared-device over-deletion), local cleanup (purgeAccountLocal + resetIdentity rotate anon/session + immediate signOut since deleted JWT stays valid till expiry). 3 migrations WRITTEN NOT APPLIED: shares+recipes reconcile (idempotent no-op on prod), app_events CASCADE defense-in-depth, pg_cron 12-month purge job (idempotent). Tests: 43 new (29 server + 14 client) + original 88 = 131/131 PASS, tsc clean, no live contact (SC_TEST_FAKE_AUTH offline seam), secret scan test now enforces no service key in src/. Files: server.js +126, events.ts +18, session.ts +19, Preferences.tsx +133, 3 migrations, 2 tests, PRIVACY-POLICY-1.6-DRAFT.html (DO NOT PUBLISH until deletion live). Report: SHARECHEF-REVISION-6.md. PENDING: Tai's npm run build for vite bundle proof → reviewer sign-off. Still true: switches OFF, HEAD 1dc159d, nothing pushed/deployed/applied.
- 2026-08-18: SILENCE HALLUCINATION FIX applied LOCALLY (Tai's Go, option A — ride along with 1.6 deploy, NOT pushed). server.js session.update audio.input: turn_detection threshold 0.6→0.7 (quiet-mic hiss was triggering VAD) + added noise_reduction {type:'near_field'} so room noise is stripped before voice detection. Root cause: near-silence fed to gpt-4o-transcribe hallucinated phantom phrases ("Hello world"/Arabic) that Micheli answered — same failure class as 07-29 whisper-1 swap; this closes the VAD-trigger side. node --check clean, all 135/135 tests pass (31+16+38+32+18). Tradeoff to watch after deploy: very soft speakers may need to speak slightly louder. Rollback: threshold 0.7→0.6, delete noise_reduction line. UNCOMMITTED — fold into next checkpoint commit. Still true: switches OFF, nothing pushed/deployed.
- 2026-08-18: Tai reports v1.5 (build 10) APPROVED by Apple ~2026-08-16 (auto-release). First iPad release; carries wake lock, forgot password, preferences, safe-area fix, clean description + SAFE FOR YOUR TABLE block. Verify on live listing: iPad screenshots/compatibility shown, description clean (old pasted chat text gone), and app updates + works on Tai's iPhone.
- 2026-08-18 (am): PASSWORD-RESET UX INCIDENT (Tai locked out on iPhone, "invalid login credentials"). Findings, all confirmed in code: (1) UNRESOLVED REPORT — Tai + users say "Forgot password?" link not visible on the app sign-in screen, yet the link IS in the 1.5 ios bundle (ios/App/App/public/assets/index-BucppbxZ.js, referenced by its index.html) and in live web (origin/main Login.tsx:63). Need a screenshot of the sign-in screen to diagnose (version mismatch vs CSS/visibility — link is muted-color 13px right-aligned; suspect low contrast on plum background). Chrome MCP was disconnected, couldn't view live page. (2) Reset links are single-use; Tai opened his link once, wandered to sign-in, link died — /reset then shows request-mode (email form) with NO explanation, looks broken. FIX CANDIDATE: show a clear "this link was already used/expired — request a new one" message when landing with #error=otp_expired. (3) ResetPassword.tsx has ONE password field, no confirm field, and iOS offered no save-to-keychain — add confirm field. (4) Supabase built-in sender rate-limits reset emails (~2/hr); repeated requests are silently dropped while the UI says "check your email" — misleading. Longstanding follow-up now urgent: custom SMTP + branded template removes the limit. (5) Tai's Passwords app had 4+ sharechef-ish entries (site, supabase.com, railway.app, test) — advised keep newest site entry only, never delete supabase/railway admin logins. Status: waiting out the rate limit (~noon), then one clean reset pass. Supabase MCP list_projects returned empty — MCP not scoped to ShareChef3; check connector account for future log access.

## 2026-08-19 — Micheli ignored saved dietary preferences (kosher violated: chicken + cheese)
Symptom: Tai created a SECOND account (new email), saved kosher in Preferences, ended the session cleanly, started a fresh session — Micheli still built a chicken-and-cheese dish. Clean retest (save -> End session -> new session) reproduced it, ruling out the "preferences only read at session start" design gap.
Root cause: the three server-side loaders — loadProfile (profiles), loadCookState (cook_sessions), loadMemory (micheli_memory) — all selected with NO .eq() owner filter and then called .maybeSingle(). With one row in the table this worked by accident. The moment a second account existed, the query could resolve more than one row, .maybeSingle() returned an error, and all three functions swallowed the error and returned null. Micheli's instructions were then assembled with dietaryRules(null) === '' — the kosher rule was never in the prompt at all. Micheli did not disobey the rule; it was never told. Same silent-null path also killed cook resume and long-term memory.
Fix (LOCAL ONLY, not committed, not deployed): added the owner filter and a userId argument to all three loaders — loadProfile .eq('id', userId), loadCookState .eq('user_id', userId), loadMemory .eq('user_id', userId); updated both call sites (recipe endpoint line ~609 passes reqUser.id; WS auth handshake line ~1054 passes user.id); loadProfile now logs loudly instead of failing silently (console.error on query error naming the user and warning that dietary rules will be missing, console.warn when no row exists). node --check clean. 135/135 tests pass (31+16+38+32+18), unchanged from baseline. Tai's uncommitted 2026-08-18 silence-hallucination fix (threshold 0.7 + noise_reduction) verified intact at lines 913/920. Backup of pre-fix file at server.js.pre-profile-fix.bak.
OPEN — NOT YET ANSWERED: why did the unfiltered query see more than one row? If RLS on profiles/cook_sessions/micheli_memory correctly scoped SELECT to the owner, an unfiltered query would have returned exactly one row and never broken. That it broke on the second account suggests those SELECT policies are NOT owner-scoped, which would be a cross-user read exposure, not just a correctness bug. The .eq() filter is correct either way but may be masking a live RLS hole. VERIFY THE SELECT POLICIES IN SUPABASE BEFORE THE PRODUCT HUNT LAUNCH.
Recurrence note: this is the THIRD failure of the class "Micheli ignores what it should know about the user" (07-26 language, 07-29 language/memory precedence, 08-19 dietary rules). The first two were prompt-precedence bugs; this one is a data-loading bug. Common thread: the loaders fail to null silently and the prompt is assembled anyway with the section missing. Structural fix to consider: refuse to open a voice session for a signed-in user whose profile load errored, or surface a visible "preferences not loaded" state in the UI, rather than cooking on with rules silently absent.
STATUS: fixed locally, NOT deployed. Live users on origin/main still hit this bug.

## 2026-08-19 — Micheli ignored kosher rule (meat + dairy)

**Symptom:** Micheli suggested a meat-and-cheese dish for a user with `kosher = true`.

**Ruled out (verified, not assumed):**
- Railway/server down — `/health` returned ok, gpt-realtime, shimmer, apiKey true
- OpenAI key/quota — HTTP 200, 124 models, gpt-realtime + gpt-4o both present
- Recipe pipeline — POST /api/recipe/generate returned valid JSON, HTTP 200
- WS proxy / realtime leg — proxy upgrade OK, `session.created` received, audio modality on
- Preferences not saving — profiles row 6493c58c had kosher=true, gluten_free=true, saved 17:05:38 EDT
- Auth handoff (branch A) — Railway logs at 17:08:15 and 17:11:05 both read `(profile loaded)`
- Profile read failure (branch B) — `loadProfile` selects kosher, celiac, allergies correctly

**Root cause:** the `micheli_memory` summary for this user contained
"enhancing dishes with cheese, especially Parmesan... melt cheese on top...
combining chicken with rice and cheese... chicken and broccoli bake with Parmesan".
It was written before kosher was enabled. `memoryInstruction()` is concatenated
AFTER `dietaryRules()` in the session.update instructions. A specific, repeated,
personal preference outweighed one abstract rule, despite the prompt line saying
dietary rules always win. Micheli followed the memory.

**Fix applied:** deleted the conflicting `micheli_memory` row (verified 0 rows left).
Backup: `micheli-memory-backup-2026-08-19.json`.

**RECURRENCE — 4th failure in this class. Patch is not enough.** Structural fixes
still open:
1. Memory writes are never validated against the user's dietary rules. A kosher
   user can accumulate meat+dairy preferences again within a few sessions.
   Fix: filter/reject conflicting content at memory-write time, not read time.
2. Dietary rules are injected as system-prompt text only. Prompt text is not an
   enforcement mechanism for a safety constraint. Celiac and severe allergies ride
   the same code path as kosher — same failure mode, higher stakes.
   Fix: send rules as a conversation item, and/or add a deterministic server-side
   check on proposed ingredients.
3. `/api/qa-chat` (server.js ~line 705) uses MICHELI_PROMPT with NO dietary rules
   at all. Test-only endpoint, but it is live in production.

- 2026-08-20: CONFIRM-PASSWORD FIELDS added LOCALLY (Tai's Go) to Signup.tsx and ResetPassword.tsx — both had a single password box; one typo at signup = locked-out user feeding the 08-18 reset mess. Both forms now have a "Confirm password" field with a match check before submit (closes 08-18 finding #3). tsc clean, all 5 test files pass, vite build verified on Tai's Mac (✓ built 393ms, 118.40KB gzip — consistent with 117KB baseline; pre-existing Capacitor chunk warnings unchanged). NOT committed, NOT deployed — rides with the 1.6 deploy train. Also 2026-08-20: root-caused Tai's 08-19 "everything broke" report — the live app runs origin/main (f287023) which predates ALL local fixes; the dietary/silence/language failures he saw are the documented old bugs, not regressions. Local main is 3 checkpoints ahead (rev5 1dc159d, rev6 97435f1, rev7 ccbb538) + uncommitted server.js fixes. SAFETY PRIORITY: the 08-19 loader bug (silent loss of dietary rules) is still live in production — deploy decision pending Tai.
